[workspace]
members = [
    "rb-loader",
    "ross-kernel",
    "ross-common",
    "ross-user/init",
    "ross-user/hello",
]
# Optionally define a resolver (highly recommended: "2" or "3")
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
